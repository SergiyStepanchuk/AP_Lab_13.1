//////////////////////////////////////////////////////////////////////////////
// dod.cpp
// файл реалізації функції
#include "dod.h"
#include "var.h" // підключили зовнішні оголошення змінних
#include <cmath>
using namespace nsVar;
void nsDod::dod() {
	a *= (pow(2 * n - 1, 2) * pow(x, 2)) / (2. * n * (2. * n + 1)); // доданок домножується на
} // коефіцієнт рекурентності